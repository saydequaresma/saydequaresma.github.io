---
export interface Props {
  type: "BlogPosting" | "Person" | "WebSite";
  data: any;
}

const { type, data } = Astro.props;

let structuredData = {};

if (type === "BlogPosting") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    headline: data.title,
    description: data.description,
    author: {
      "@type": "Person",
      name: data.author || "Quaresma",
      url: "https://saydequaresma.github.io/about",
    },
    datePublished: data.pubDatetime.toISOString(),
    dateModified: data.modDatetime?.toISOString() || data.pubDatetime.toISOString(),
    publisher: {
      "@type": "Person",
      name: "Quaresma",
      logo: {
        "@type": "ImageObject",
        url: "https://saydequaresma.github.io/avatar.jpg",
      },
    },
    mainEntityOfPage: {
      "@type": "WebPage",
      "@id": data.url,
    },
    image: data.ogImage || "https://saydequaresma.github.io/avatar.jpg",
    articleSection: data.tags?.[0] || "Technology",
    keywords: data.tags?.join(", ") || "",
    wordCount: data.wordCount,
    timeRequired: data.readingTime,
  };
} else if (type === "Person") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "Person",
    name: "Quaresma",
    url: "https://saydequaresma.github.io",
    image: "https://saydequaresma.github.io/avatar.jpg",
    sameAs: [
      "https://github.com/saydequaresma",
    ],
    jobTitle: "Solo Builder",
    description:
      "Solo builder making AI tools for small operators that bigger SaaS won't bother with.",
  };
} else if (type === "WebSite") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: "Quaresma",
    url: "https://saydequaresma.github.io",
    description:
      "Solo builder making AI tools for small operators that bigger SaaS won't bother with.",
    author: {
      "@type": "Person",
      name: "Quaresma",
    },
    potentialAction: {
      "@type": "SearchAction",
      target: "https://saydequaresma.github.io/search?q={search_term_string}",
      "query-input": "required name=search_term_string",
    },
  };
}
---

<script type="application/ld+json" set:html={JSON.stringify(structuredData)} />