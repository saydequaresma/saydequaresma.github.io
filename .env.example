# MailerLite — preenche com IDs reais em .env (não commitado).
# Vês os IDs no painel: Forms → Embedded form → no atributo `action` do <form>.
# Em produção, são injectados pelo workflow a partir de GitHub Repository Variables.
PUBLIC_MAILERLITE_ACCOUNT=
PUBLIC_MAILERLITE_FORM=
